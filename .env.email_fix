# Email Configuration - Local SMTP (on same server)
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=admin@atomgameshop.com
SMTP_PASS=Lukonehtan001!
SMTP_SECURE=false
SMTP_IGNORE_TLS=true
SMTP_REQUIRE_TLS=false
